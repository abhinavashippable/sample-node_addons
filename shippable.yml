# Language setting
language: "node_js"
  

# Version number
node_js:
  - 0.12

branches:
 except:
   - "pr2-master"

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  #pre_ci:
   # - docker build -t=quay.io/shippablequay/sample_node_pdr .

     
